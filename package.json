{
  "name": "shootem-up-game",
  "version": "1.0.0",
  "description": "A fast, tactile voxel first-person arena shooter built for the browser.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "start": "node server/index.js",
    "preview": "npm run build && npm run start",
    "test": "node --test",
    "test:browser": "node qa/smoke.mjs",
    "visual:review": "node qa/visual-review.mjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "vite": "^8.1.5"
  },
  "engines": {
    "node": ">=22"
  }
}
